class BedPart:
    def __init__(self, x_min, x_max, y_min, y_max, plant):
        self.x_min = x_min
        self.x_max = x_max
        self.y_min = y_min
        self.y_max = y_max
        self.plant = plant
